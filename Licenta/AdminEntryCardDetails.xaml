<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Licenta.AdminEntryCardDetails"
             Title="AdminEntryCardDetails">
    <VerticalStackLayout Spacing="1" Margin="5">
        <StackLayout Margin="10" >
            <Editor Placeholder="Enter the card number" Text="{Binding CardNumber}"
                    HeightRequest="20"/>
            <Editor Placeholder="Enter the name of the card holder" Text="{Binding CardHolder }"
                    HeightRequest="20" />
            <Editor Placeholder="Enter the expiration date" Text="{Binding ValidUntil }"
                    HeightRequest="20" />
            <Editor Placeholder="Enter the security code (CVV)" Text="{Binding SecurityCode }"
                    HeightRequest="20" />
            <Editor Placeholder="Enter the email of the card holder" Text="{Binding Email  }"
                    HeightRequest="20" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Text="Save" Clicked="OnSaveButtonClicked" />
                <Button Grid.Column="1" Text="Delete" Clicked="OnDeleteButtonClicked"/>
            </Grid>
        </StackLayout>
    </VerticalStackLayout>

</ContentPage>